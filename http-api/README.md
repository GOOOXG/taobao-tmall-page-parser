# ??/?????? HTTP API

Node ??????????? `taobao-tmall-page-parser.mjs`??????? JSON?

```text
shopInfo -> spuInfo -> skuInfo -> detailPageInfo
```

?? Node.js 20 ??????????? API Token?????? `127.0.0.1`?

## ????

| ?? | ?? | ?????????? |
| --- | --- | --- |
| `managed` | Linux ????? | ??????? Chromium?Profile ?????? |
| `cdp` | ?????? | ???????????? Chrome CDP |

????????? `managed`?????????????? Chrome ??? Profile?

## ?????

??????? Linux ??????

```bash
cd /opt/taobao-tmall-page-parser/http-api
npm ci
npx playwright install --with-deps chromium
sudo mkdir -p /var/lib/taobao-parser/chrome-profile
sudo chown -R "$(id -u):$(id -g)" /var/lib/taobao-parser
```

?????? API?

```bash
export PARSER_BROWSER_MODE=managed
export PARSER_CHROME_PROFILE_DIR=/var/lib/taobao-parser/chrome-profile
export PARSER_HEADLESS=true
export PARSER_HOST=127.0.0.1
export PARSER_PORT=3210
npm start
```

????????? `/login`?`/login/status` ? `/parse` ???????? Chromium?

## ????????

??????????????????

```text
https://????/login
```

??????????????????????????????????????????????????????????????????????????????

????????????????

```text
http://127.0.0.1:3210/login
```

????????????????? SSH ??????????????????

```bash
ssh -L 3210:127.0.0.1:3210 user@server.example.com
```

?????????????

```text
http://127.0.0.1:3210/login
```

??????Cookie ???? Chromium ?????

```text
/var/lib/taobao-parser/chrome-profile
```

???????? Cookie??????????????????????????????????

### ????

????? DNS ??????????? Nginx ???????? `3210` ??????? [nginx-taobao-parser.conf.example](nginx-taobao-parser.conf.example) ???????????

```bash
sudo cp nginx-taobao-parser.conf.example /etc/nginx/conf.d/taobao-parser.conf
sudo nano /etc/nginx/conf.d/taobao-parser.conf
sudo nginx -t
sudo systemctl reload nginx
```

????? `mjs.example.com` ?????????? Certbot ??????? HTTPS???????

```text
https://????/login
```

???????????????????????????????????????`/login/qrcode` ? `/login/status` ?????????????????????

## HTTP ??

| ?? | ?? | ?? |
| --- | --- | --- |
| `GET` | `/health` | ?? API ?? |
| `GET` | `/login` | ?????????? |
| `GET` | `/login/status` | ??????????? |
| `GET` | `/login/qrcode` | ?????????? PNG |
| `POST` | `/logout` | ???? Profile ???? Cookie |
| `POST` | `/parse` | ???? ID ???? |

???????

```bash
curl -sS http://127.0.0.1:3210/login/status
```

```json
{
  "code": 0,
  "message": null,
  "data": {
    "state": "authenticated",
    "loginPageOpen": false,
    "browserMode": "managed"
  },
  "recordTime": "2026-07-27T00:00:00.000Z"
}
```

`state` ????

```text
unauthenticated
pending
authenticated
```

## ????

????? 6 ? 20 ??? ID?????? URL?

```bash
curl -sS \
  --request POST \
  http://127.0.0.1:3210/parse \
  --header 'Content-Type: application/json' \
  --data '{"itemId":"901024796701"}'
```

???? JSON?

```bash
curl -sS \
  --request POST \
  http://127.0.0.1:3210/parse \
  --header 'Content-Type: application/json' \
  --data '{"itemId":"901024796701"}' \
  --output item-901024796701.json
```

??????????? Chromium ??????????????????? MJS?????????Chromium ??? Profile ???????????

## ????

```bash
curl -sS --request POST http://127.0.0.1:3210/logout
```

??????? Profile ? Cookie??????????????????????? `PARSER_CHROME_PROFILE_DIR` ?????

## systemd

???? [taobao-parser.service.example](taobao-parser.service.example)?????? `User`???? Node ??????

```bash
sudo cp taobao-parser.service.example /etc/systemd/system/taobao-parser.service
sudo systemctl daemon-reload
sudo systemctl enable --now taobao-parser
sudo systemctl status taobao-parser
```

?????

```bash
journalctl -u taobao-parser -f
```

??????

```bash
sudo systemctl restart taobao-parser
sudo systemctl stop taobao-parser
```

## ????

| ?? | ??? | ?? |
| --- | --- | --- |
| `PARSER_BROWSER_MODE` | `cdp` | ????? `managed` |
| `PARSER_CHROME_PROFILE_DIR` | ? | `managed` ?????? Profile ?? |
| `PARSER_HEADLESS` | `true` | ?? Chromium ??????? |
| `PARSER_CHROME_EXECUTABLE_PATH` | Playwright Chromium | ????? Chromium ?? |
| `PARSER_HOST` | `127.0.0.1` | API ???? |
| `PARSER_PORT` | `3210` | API ?? |
| `TAOBAO_CDP_URL` | `http://127.0.0.1:9224` | ? `cdp` ???? |

## ?? CDP ????

?????????? Chrome ??

```powershell
& "$env:ProgramFiles\Google\Chrome\Application\chrome.exe" `
  --remote-debugging-port=9224 `
  --user-data-dir="C:\tmp\taobao-parser-profile"

$env:PARSER_BROWSER_MODE = "cdp"
$env:TAOBAO_CDP_URL = "http://127.0.0.1:9224"
npm start
```

???? Docker ?????????

## ????

????????? API Token?`/login`?`/logout` ? `/parse` ??????????????????

???? `127.0.0.1`??? SSH ????? VPN ????? IP ????????? `PARSER_HOST=0.0.0.0`????????????????????????

????? CORS??????????? URL????????? Cookie?

## ????

| HTTP | ?? |
| --- | --- |
| `400` | ?? ID ? JSON ???? |
| `409` | ???? `/login` ?????? |
| `415` | Content-Type ?? `application/json` |
| `502` | ??/???????????? |
| `503` | Chromium ????? CDP ???? |
