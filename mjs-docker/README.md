# Docker ??

???? HTTP API?Playwright ? Chromium ???????????????????????????? Chrome????????????? Profile?

??????

```text
Node HTTP API
Playwright 1.62.0
Chromium
/data/chrome-profile ???????
```

?? JSON ??? MJS ?????

```text
shopInfo -> spuInfo -> skuInfo -> detailPageInfo
```

## ??

- Linux ???
- Docker Engine 24 ?????
- Docker Compose v2
- ?? 2 ? CPU?4 GB ???20 GB ????

## ??

?????????

```bash
cd mjs-docker
cp .env.example .env
docker compose up -d --build
```

????????

```bash
docker compose ps
docker compose logs -f api
```

?????

```bash
curl -sS http://127.0.0.1:3210/health
```

?????

```json
{
  "status": "ok",
  "service": "taobao-tmall-parser",
  "browserMode": "managed"
}
```

## ????????

????????? HTTPS ???????????

```text
https://????/login
```

??????????????????????????????????????????????????

??????????????? `http-api/nginx-taobao-parser.conf.example`????????? `http://127.0.0.1:3210`?

??????????????????? `127.0.0.1`???????? SSH ???

```bash
ssh -L 3210:127.0.0.1:3210 user@server.example.com
```

?????????????

```text
http://127.0.0.1:3210/login
```

????????????????????????????????Cookie ?????? Docker Volume?

```text
taobao-tmall-parser-profile
```

???????????? Volume??????????????????? Cookie?

???????

```bash
curl -sS http://127.0.0.1:3210/login/status
```

## ????

```bash
curl -sS \
  --request POST \
  http://127.0.0.1:3210/parse \
  --header 'Content-Type: application/json' \
  --data '{"itemId":"901024796701"}'
```

?????

```bash
curl -sS \
  --request POST \
  http://127.0.0.1:3210/parse \
  --header 'Content-Type: application/json' \
  --data '{"itemId":"901024796701"}' \
  --output item-901024796701.json
```

## ????

```bash
curl -sS --request POST http://127.0.0.1:3210/logout
```

??????? `/login` ?????????

## ?????

```bash
docker compose restart api
docker compose stop
docker compose start
```

???????????? Profile?

```bash
docker compose down
```

???? `docker compose down -v`??????????? Cookie ? Volume?

## ??

```bash
git pull
cd mjs-docker
docker compose up -d --build
```

?????????? Volume?

## ?? Profile

```bash
docker run --rm \
  --volume taobao-tmall-parser-profile:/data:ro \
  --volume "$PWD:/backup" \
  alpine \
  tar -czf /backup/profile-backup-$(date +%Y%m%d-%H%M%S).tar.gz -C /data .
```

???????? Cookie??????????????? GitHub ????????

## ????

?? `.env`?

```text
HOST_PORT=3211
```

???????

```bash
docker compose up -d
```

???????? `http://127.0.0.1:3211`?

## ????

| ?? | ?? |
| --- | --- |
| Chromium Profile | Volume `taobao-tmall-parser-profile` |
| API ? MJS | ?? `/app` |
| ??? Profile | `/data/chrome-profile` |
| Compose ?? | `mjs-docker/compose.yaml` |

## ????

????????? API Token?Compose ???????? `127.0.0.1`?????????? `0.0.0.0:3210:3210` ????????

???? SSH ????? VPN ???????????????? IP ??????? `/login`?`/logout` ? `/parse` ?????????

???? root ?? `pwuser` ?? Chromium???? Playwright ?? seccomp ???????????????????????

## ????

???????

```bash
docker compose ps
docker inspect --format='{{json .State.Health}}' taobao-tmall-parser-api-1
```

???????

```bash
docker compose logs --tail=200 api
```

?????

| ?? | ?? |
| --- | --- |
| `/health` ?? | ????????? |
| `/login` ?? 502 | ????????????? |
| ?????? | ?? `/login`??????? |
| ?????? | ????????????? |
| Chromium ???? | ?? `shm_size`?seccomp ??? Volume ?? |
| ???? 502 | ????????? ID??????? |
